---
- name: Search-Only, return list of found updates
  hosts: rest_laptops
  vars:
    ansible_remote_tmp: C:\Users\hcso\tmp\.ansible\tmp
  tasks:
    - name: search for updates
      ansible.windows.win_updates:
        category_names:
          - CriticalUpdates
          - SecurityUpdates
          - UpdateRollUps
        state: searched
        log_path: '%USERPROFILE%\ansible.txt'
